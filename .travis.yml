language: python
sudo: false

cache:
  apt: true
  directories:
  - $HOME/.cache/pip
  - $HOME/download
python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
before_install:
  - pip install pip -U
  - pip install pytest -U
install:
  - travis_retry pip install -e .
script: 
  - travis_wait pytest test.py
    
after_success: 
  - codecov 
cache: 
    apt: true
    directories:
        - $HOME/.pip-cache

